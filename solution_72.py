from math import gcd

if __name__ == '__main__':
    limit = 1_000_000
    frac_counter = 0
    for enumerator in range(1, limit):
        for i in range(enumerator+1, limit):
            if gcd()
    print(frac_counter)